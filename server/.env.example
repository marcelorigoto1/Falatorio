# Copie este arquivo para ".env" e preencha.
# O arquivo .env fica de fora do Git (veja o .gitignore) — nunca suba senhas
# para o repositorio. No Render, use Environment em vez deste arquivo.

# Senha para entrar na sala. Vazio ou ausente = sala aberta.
ROOM_PASSWORD=

# Quantas pessoas cabem na sala ao mesmo tempo (padrao 12).
MAX_USERS=12

# Chave da API do YouTube, para buscar musica pelo NOME.
# Sem ela, ainda da para colar o link do video, que funciona normalmente.
# Como pegar: console.cloud.google.com -> criar projeto -> ativar
# "YouTube Data API v3" -> Credenciais -> Criar chave de API.
YOUTUBE_API_KEY=
